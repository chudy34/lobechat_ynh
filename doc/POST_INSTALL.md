Lobe Chat is installed behind YunoHost's nginx reverse proxy.

If you did not configure a default provider API key during installation, configure your model provider from Lobe Chat after opening the app.
